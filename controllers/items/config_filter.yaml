# ===================================
# Filter Scope Definitions
# ===================================
scopes:

    source:

        # Filter name
        label: khalilthiero.rssfetcher::lang.source.source

        # Model Class name
        modelClass: Khalilthiero\RssFetcher\Models\Source

        # Model attribute to display for the name
        nameFrom: name

        # Apply query scope
        scope: FilterSources

    is_published:

        # Filter name
        label: khalilthiero.rssfetcher::lang.item.hide_published

        # Filter type
        type: checkbox

        # SQL Conditions
        conditions: is_published <> 1
